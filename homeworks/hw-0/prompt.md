# Homework 0 Prompts

Этот файл для команд/запросов, которые ты будешь давать мне для работы над кодом проекта.

## Prompts

### РП 1 - Проект на эликсир + экто

1. Давай проработаем РП 1 из WORKPLAN.md внутри ai-swe. Напиши что собираешься сделать, только после моего согласия выполни

### РП 2 - Добавить базовую audit-таблицу и schema

1. давай в проекте создадим папку specs, будем там делать описания РП по проекту, сразу туда добавим файл wp-2-migration-schema.md для РП 2 из нанего плана по проекту

2. Давай в описании таблицы audit_events поменяем немного колонки:
- убирём actor_id (эта информация скорее всего будет в метаданных)
- для id сделаем тип данных uuid
- вместо occurred_at будем использовать системные колонки, которые предоставляет экто через timestamps() и там нас обновление не интересует, а колонка которая на вставку как раз подойдёт на замену поля occurred_at, посмотри как там оставить только колонку на вставку
- для колонок operation и entity_type,  тип text заменим на строковый, кажется избыточно использовать text, когда там будут не большие строки 

3. давай ещё добавим в описание блок про требвания к миграции. Т.е. мы делаем библиотеку, хотелось бы предоставить пользователю интерфейс запуска миграции. Например, я как пользователь хочу сгенирировать в своём проекту пустую миграцию add_audit_table и потом в файле миграции заполнить функции up/down вот так
```elixir
def up do
  Audit.Migrations.up()
end

def down do
 Audit.Migrations.down()
end
```
и механизм шагов подключения библиотеки надо будет описать в readme самой библиотеки 

4. давай по шагам будем реализовывать код РП-2, сначала код миграции, потом код схемы, потом опишем readme

5. По шагу 1: поле id должно быть обязательным раз это первичный ключ, индексы давай уберём
По шагу 2: давай changeset сразу добавим, все поля обязательны для вставки

6. давай тесты минимальные напишем

7. тест на миграции не нужен, удали. А тест для схемы сделай по критерию готовности №3 из описания

8. Поищи как можно настроить mix проект, когда в нём есть работа с БД и мы её хотим через тесты проверять (там должна быть секция aliases для mix.exs и в ней команды для создания БД, накатывания миграций и запуска тестов)

9. для локального запуска PostgreSQL давай напишем файл docker-compose.yml который будет поднимать образ postgres с дефолтными настройками доступа к БД, но положим его на уровне папки ai-swe

10. давай поднимем постгрес и запустим тесты

11. хорошо, давай поменим wp-2 выполненным, закоммитим и запушим все изменения

12. давай закоммитим и запушим файлы в hw-0 по РП 2
